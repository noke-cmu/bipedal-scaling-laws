# Python dependencies for the bipedal scaling simulations.
#
# Install Drake first, into a virtual environment (it provides pydrake and pulls
# in a compatible numpy):
#
#     pip install drake
#     pip install -r requirements.txt
#
# See https://drake.mit.edu/pip.html for platform notes.
#
# Note: do NOT pin numpy below 2 — current Drake releases require numpy >= 2.
numpy>=1.24
pandas>=2.2
matplotlib>=3.8
scipy>=1.11
joblib>=1.3
requests>=2.28
ipython>=8.0
